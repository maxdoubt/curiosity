<%= form_for(@gradelevel) do |f| %>
  <% if @gradelevel.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@gradelevel.errors.count, "error") %> prohibited this gradelevel from being saved:</h2>

      <ul>
      <% @gradelevel.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_area :name %>
  </div>
  <div class="field">
    <%= f.label :abbrev %><br>
    <%= f.text_area :abbrev %>
  </div>
  <div class="field">
    <%= f.label :number %><br>
    <%= f.number_field :number %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
