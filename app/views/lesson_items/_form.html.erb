<%= form_for(@lesson_item) do |f| %>
  <% if @lesson_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@lesson_item.errors.count, "error") %> prohibited this lesson_item from being saved:</h2>

      <ul>
      <% @lesson_item.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :content %>
    <%= f.text_area :content, :class => 'form-control' %>
  </div>
  
  <%= f.hidden_field :lesson_template_box_id, :value => @box.id %>
  <%= f.hidden_field :lesson_id, :value => @lesson.id %>
  
  <div class="actions">
    <%= f.submit 'Add Content', :class => 'btn btn-default btn-xs' %>
  </div>
<% end %>
